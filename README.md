#Простой REST API с использованием JSON

* GET api/employees возвращает всех работников
* GET api/employees/{id} возвращает работника по его ID (обрабатываются исключения в случае ввода некорректного ID)